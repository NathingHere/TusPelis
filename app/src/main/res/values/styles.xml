<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="CampoRegistro" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/botones</item>
        <item name="colorControlActivated">@color/botones</item>
        <item name="android:textColorHighlight">@color/botones</item>
        <item name="android:textColorHint">@color/botones</item>

    </style>

</resources>
